((nil .
     ((make-variable-buffer-local . 'compile-command)
      (compile-command . (format "gcc -o %s %s && .%s" (file-name-sans-extension buffer-file-name) buffer-file-name (file-name-base buffer-file-name))))))
