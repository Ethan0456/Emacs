#include <bits/stdc++.h>
using namespace std;

int gcd(int a, int b){
  if (b==0) return a;
  return gcd(b, a%b);
}

int func(int a, int b){
  if ((a*b)/(gcd(a,b)*gcd(a,b)) <= 3){
    if (a != b) return 2;
    return 1;
  }
  return 0;
}

int main(){
  int tests;
  cin >> tests;
  while (tests--){
    int pairs = 0;
    int num;
    cin >> num;
    if (num == 1){
      cout << 1 << endl;
      return 0;
    }

    
  }
}
