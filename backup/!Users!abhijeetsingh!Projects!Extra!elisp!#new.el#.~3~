(setq num 10)
(setq flag 0)
(setq countOuter 2)
(while (<= countOuter num)
  (setq countInner 2)
  (while (<= countInner countOuter)
    (when (equal (% countOuter countInner) 0)
      (setq flag 1))
    (1+ countInner))
  (if (equal flag 1)
	    (message "%d not Prime" countOuter)
            (message "%d Prime" countOuter))
  (1+ countOuter))
